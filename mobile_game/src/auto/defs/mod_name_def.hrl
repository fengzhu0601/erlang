%% Do not edit it.
%% Auto Generate by ffsj autotools
-ifndef(robot_fsm). 
-define(robot_fsm, robot_fsm).
 -endif.

-ifndef(robot_new). 
-define(robot_new, robot_new).
 -endif.

-ifndef(robot_new_server). 
-define(robot_new_server, robot_new_server).
 -endif.

-ifndef(robot_new_sup). 
-define(robot_new_sup, robot_new_sup).
 -endif.

-ifndef(mobile_game_cmd). 
-define(mobile_game_cmd, mobile_game_cmd).
 -endif.

-ifndef(robot_item_sproto). 
-define(robot_item_sproto, robot_item_sproto).
 -endif.

-ifndef(robot_debug_sproto). 
-define(robot_debug_sproto, robot_debug_sproto).
 -endif.

-ifndef(robot_player_sproto). 
-define(robot_player_sproto, robot_player_sproto).
 -endif.

-ifndef(robot_proto_info). 
-define(robot_proto_info, robot_proto_info).
 -endif.

-ifndef(err_info_def). 
-define(err_info_def, err_info_def).
 -endif.

-ifndef(scene_def). 
-define(scene_def, scene_def).
 -endif.

-ifndef(guild_def). 
-define(guild_def, guild_def).
 -endif.

-ifndef(player_def). 
-define(player_def, player_def).
 -endif.

-ifndef(item_bucket_def). 
-define(item_bucket_def, item_bucket_def).
 -endif.

-ifndef(task_def). 
-define(task_def, task_def).
 -endif.

-ifndef(scene_monster_def). 
-define(scene_monster_def, scene_monster_def).
 -endif.

-ifndef(game_def). 
-define(game_def, game_def).
 -endif.

-ifndef(gm_def). 
-define(gm_def, gm_def).
 -endif.

-ifndef(main_star_shop_sproto). 
-define(main_star_shop_sproto, main_star_shop_sproto).
 -endif.

-ifndef(gongcheng_sproto). 
-define(gongcheng_sproto, gongcheng_sproto).
 -endif.

-ifndef(dig_sproto). 
-define(dig_sproto, dig_sproto).
 -endif.

-ifndef(alchemy_sproto). 
-define(alchemy_sproto, alchemy_sproto).
 -endif.

-ifndef(sky_ins_sproto). 
-define(sky_ins_sproto, sky_ins_sproto).
 -endif.

-ifndef(scene_sproto). 
-define(scene_sproto, scene_sproto).
 -endif.

-ifndef(honest_user_sproto). 
-define(honest_user_sproto, honest_user_sproto).
 -endif.

-ifndef(titles_sproto). 
-define(titles_sproto, titles_sproto).
 -endif.

-ifndef(chat_sproto). 
-define(chat_sproto, chat_sproto).
 -endif.

-ifndef(debug_sproto). 
-define(debug_sproto, debug_sproto).
 -endif.

-ifndef(mall_sproto). 
-define(mall_sproto, mall_sproto).
 -endif.

-ifndef(guild_mining_sproto). 
-define(guild_mining_sproto, guild_mining_sproto).
 -endif.

-ifndef(gm_sproto). 
-define(gm_sproto, gm_sproto).
 -endif.

-ifndef(vip_new_sproto). 
-define(vip_new_sproto, vip_new_sproto).
 -endif.

-ifndef(camp_sproto). 
-define(camp_sproto, camp_sproto).
 -endif.

-ifndef(rank_sproto). 
-define(rank_sproto, rank_sproto).
 -endif.

-ifndef(auction_sproto). 
-define(auction_sproto, auction_sproto).
 -endif.

-ifndef(task_sproto). 
-define(task_sproto, task_sproto).
 -endif.

-ifndef(course_sproto). 
-define(course_sproto, course_sproto).
 -endif.

-ifndef(crown_sproto). 
-define(crown_sproto, crown_sproto).
 -endif.

-ifndef(daily_activity_sproto). 
-define(daily_activity_sproto, daily_activity_sproto).
 -endif.

-ifndef(recharge_reward_sproto). 
-define(recharge_reward_sproto, recharge_reward_sproto).
 -endif.

-ifndef(mail_sproto). 
-define(mail_sproto, mail_sproto).
 -endif.

-ifndef(phase_achievement_sproto). 
-define(phase_achievement_sproto, phase_achievement_sproto).
 -endif.

-ifndef(server_login_prize_sproto). 
-define(server_login_prize_sproto, server_login_prize_sproto).
 -endif.

-ifndef(nine_lottery_sproto). 
-define(nine_lottery_sproto, nine_lottery_sproto).
 -endif.

-ifndef(card_sproto). 
-define(card_sproto, card_sproto).
 -endif.

-ifndef(sinks_state_sproto). 
-define(sinks_state_sproto, sinks_state_sproto).
 -endif.

-ifndef(version_sproto). 
-define(version_sproto, version_sproto).
 -endif.

-ifndef(arena_sproto). 
-define(arena_sproto, arena_sproto).
 -endif.

-ifndef(login_prize_sproto). 
-define(login_prize_sproto, login_prize_sproto).
 -endif.

-ifndef(pet_new_sproto). 
-define(pet_new_sproto, pet_new_sproto).
 -endif.

-ifndef(pet_sproto). 
-define(pet_sproto, pet_sproto).
 -endif.

-ifndef(open_server_happy_sproto). 
-define(open_server_happy_sproto, open_server_happy_sproto).
 -endif.

-ifndef(ride_sproto). 
-define(ride_sproto, ride_sproto).
 -endif.

-ifndef(shop_sproto). 
-define(shop_sproto, shop_sproto).
 -endif.

-ifndef(impact_ranking_list_sproto). 
-define(impact_ranking_list_sproto, impact_ranking_list_sproto).
 -endif.

-ifndef(abyss_sproto). 
-define(abyss_sproto, abyss_sproto).
 -endif.

-ifndef(skill_sproto). 
-define(skill_sproto, skill_sproto).
 -endif.

-ifndef(team_sproto). 
-define(team_sproto, team_sproto).
 -endif.

-ifndef(accomplishments_sproto). 
-define(accomplishments_sproto, accomplishments_sproto).
 -endif.

-ifndef(seller_sproto). 
-define(seller_sproto, seller_sproto).
 -endif.

-ifndef(friend_sproto). 
-define(friend_sproto, friend_sproto).
 -endif.

-ifndef(guild_sproto). 
-define(guild_sproto, guild_sproto).
 -endif.

-ifndef(crown_new_sproto). 
-define(crown_new_sproto, crown_new_sproto).
 -endif.

-ifndef(equip_sproto). 
-define(equip_sproto, equip_sproto).
 -endif.

-ifndef(player_sproto). 
-define(player_sproto, player_sproto).
 -endif.

-ifndef(item_sproto). 
-define(item_sproto, item_sproto).
 -endif.

-ifndef(card_new_sproto). 
-define(card_new_sproto, card_new_sproto).
 -endif.

-ifndef(bounty_sproto). 
-define(bounty_sproto, bounty_sproto).
 -endif.

-ifndef(gem_sproto). 
-define(gem_sproto, gem_sproto).
 -endif.

-ifndef(main_instance_sproto). 
-define(main_instance_sproto, main_instance_sproto).
 -endif.

-ifndef(proto_info). 
-define(proto_info, proto_info).
 -endif.

-ifndef(version). 
-define(version, version).
 -endif.

-ifndef(db_repair). 
-define(db_repair, db_repair).
 -endif.

-ifndef(global_data). 
-define(global_data, global_data).
 -endif.

-ifndef(ret). 
-define(ret, ret).
 -endif.

-ifndef(util). 
-define(util, util).
 -endif.

-ifndef(my_sup). 
-define(my_sup, my_sup).
 -endif.

-ifndef(timer_server). 
-define(timer_server, timer_server).
 -endif.

-ifndef(rand_util). 
-define(rand_util, rand_util).
 -endif.

-ifndef(cd_key). 
-define(cd_key, cd_key).
 -endif.

-ifndef(evt_util). 
-define(evt_util, evt_util).
 -endif.

-ifndef(util_timer). 
-define(util_timer, util_timer).
 -endif.

-ifndef(otp_util). 
-define(otp_util, otp_util).
 -endif.

-ifndef(limit_value_eng). 
-define(limit_value_eng, limit_value_eng).
 -endif.

-ifndef(utf8_file). 
-define(utf8_file, utf8_file).
 -endif.

-ifndef(my_wk). 
-define(my_wk, my_wk).
 -endif.

-ifndef(porsche_gearbox). 
-define(porsche_gearbox, porsche_gearbox).
 -endif.

-ifndef(load_rule_chart). 
-define(load_rule_chart, load_rule_chart).
 -endif.

-ifndef(debug). 
-define(debug, debug).
 -endif.

-ifndef(load_db_misc). 
-define(load_db_misc, load_db_misc).
 -endif.

-ifndef(player_data_db). 
-define(player_data_db, player_data_db).
 -endif.

-ifndef(load_db_guild_first_kill). 
-define(load_db_guild_first_kill, load_db_guild_first_kill).
 -endif.

-ifndef(load_db_main_ins). 
-define(load_db_main_ins, load_db_main_ins).
 -endif.

-ifndef(arena_log_db). 
-define(arena_log_db, arena_log_db).
 -endif.

-ifndef(platfrom). 
-define(platfrom, platfrom).
 -endif.

-ifndef(dirty_chars). 
-define(dirty_chars, dirty_chars).
 -endif.

-ifndef(model_box). 
-define(model_box, model_box).
 -endif.

-ifndef(world). 
-define(world, world).
 -endif.

-ifndef(command). 
-define(command, command).
 -endif.

-ifndef(gen_id). 
-define(gen_id, gen_id).
 -endif.

-ifndef(system_log). 
-define(system_log, system_log).
 -endif.

-ifndef(player_log_service). 
-define(player_log_service, player_log_service).
 -endif.

-ifndef(info_log). 
-define(info_log, info_log).
 -endif.

-ifndef(safe_ets). 
-define(safe_ets, safe_ets).
 -endif.

-ifndef(title_service). 
-define(title_service, title_service).
 -endif.

-ifndef(msg_service). 
-define(msg_service, msg_service).
 -endif.

-ifndef(erlang_api). 
-define(erlang_api, erlang_api).
 -endif.

-ifndef(mst_ai_sys). 
-define(mst_ai_sys, mst_ai_sys).
 -endif.

-ifndef(mst_ai_lua). 
-define(mst_ai_lua, mst_ai_lua).
 -endif.

-ifndef(mst_ai_plug). 
-define(mst_ai_plug, mst_ai_plug).
 -endif.

-ifndef(situation_calculate). 
-define(situation_calculate, situation_calculate).
 -endif.

-ifndef(room_scene). 
-define(room_scene, room_scene).
 -endif.

-ifndef(room_sup). 
-define(room_sup, room_sup).
 -endif.

-ifndef(room_system). 
-define(room_system, room_system).
 -endif.

-ifndef(room_map). 
-define(room_map, room_map).
 -endif.

-ifndef(player_room_part). 
-define(player_room_part, player_room_part).
 -endif.

-ifndef(rm_monster). 
-define(rm_monster, rm_monster).
 -endif.

-ifndef(rm_system). 
-define(rm_system, rm_system).
 -endif.

-ifndef(rm_user_evt). 
-define(rm_user_evt, rm_user_evt).
 -endif.

-ifndef(rm_debug). 
-define(rm_debug, rm_debug).
 -endif.

-ifndef(scene_event_callback). 
-define(scene_event_callback, scene_event_callback).
 -endif.

-ifndef(course_mng). 
-define(course_mng, course_mng).
 -endif.

-ifndef(friend). 
-define(friend, friend).
 -endif.

-ifndef(friend_gift_svr). 
-define(friend_gift_svr, friend_gift_svr).
 -endif.

-ifndef(friend_mng). 
-define(friend_mng, friend_mng).
 -endif.

-ifndef(auction_new_svr). 
-define(auction_new_svr, auction_new_svr).
 -endif.

-ifndef(auction_new_mng). 
-define(auction_new_mng, auction_new_mng).
 -endif.

-ifndef(ranking_lib). 
-define(ranking_lib, ranking_lib).
 -endif.

-ifndef(ranking_lib_test). 
-define(ranking_lib_test, ranking_lib_test).
 -endif.

-ifndef(tcp_eng). 
-define(tcp_eng, tcp_eng).
 -endif.

-ifndef(player_eng). 
-define(player_eng, player_eng).
 -endif.

-ifndef(timer_eng). 
-define(timer_eng, timer_eng).
 -endif.

-ifndef(day_reset). 
-define(day_reset, day_reset).
 -endif.

-ifndef(player_mod). 
-define(player_mod, player_mod).
 -endif.

-ifndef(month_reset). 
-define(month_reset, month_reset).
 -endif.

-ifndef(player_debug). 
-define(player_debug, player_debug).
 -endif.

-ifndef(task_tgr). 
-define(task_tgr, task_tgr).
 -endif.

-ifndef(err_info). 
-define(err_info, err_info).
 -endif.

-ifndef(week_reset). 
-define(week_reset, week_reset).
 -endif.

-ifndef(player_util). 
-define(player_util, player_util).
 -endif.

-ifndef(test_callback). 
-define(test_callback, test_callback).
 -endif.

-ifndef(handle_client). 
-define(handle_client, handle_client).
 -endif.

-ifndef(item_equip). 
-define(item_equip, item_equip).
 -endif.

-ifndef(equip_buf). 
-define(equip_buf, equip_buf).
 -endif.

-ifndef(item_goods). 
-define(item_goods, item_goods).
 -endif.

-ifndef(item_new). 
-define(item_new, item_new).
 -endif.

-ifndef(event_system). 
-define(event_system, event_system).
 -endif.

-ifndef(event_eng). 
-define(event_eng, event_eng).
 -endif.

-ifndef(card_new_mng). 
-define(card_new_mng, card_new_mng).
 -endif.

-ifndef(shapeshift_mng). 
-define(shapeshift_mng, shapeshift_mng).
 -endif.

-ifndef(daily_activity_service). 
-define(daily_activity_service, daily_activity_service).
 -endif.

-ifndef(daily_activity_mng). 
-define(daily_activity_mng, daily_activity_mng).
 -endif.

-ifndef(diaoyu_mng). 
-define(diaoyu_mng, diaoyu_mng).
 -endif.

-ifndef(daily_activity_handle_client). 
-define(daily_activity_handle_client, daily_activity_handle_client).
 -endif.

-ifndef(task_system). 
-define(task_system, task_system).
 -endif.

-ifndef(daily_task_tool). 
-define(daily_task_tool, daily_task_tool).
 -endif.

-ifndef(kill_monster_tgr). 
-define(kill_monster_tgr, kill_monster_tgr).
 -endif.

-ifndef(collect_thing_tgr). 
-define(collect_thing_tgr, collect_thing_tgr).
 -endif.

-ifndef(mount_tgr). 
-define(mount_tgr, mount_tgr).
 -endif.

-ifndef(blessing_tgr). 
-define(blessing_tgr, blessing_tgr).
 -endif.

-ifndef(finish_task_trigger_tgr). 
-define(finish_task_trigger_tgr, finish_task_trigger_tgr).
 -endif.

-ifndef(prize_tgr). 
-define(prize_tgr, prize_tgr).
 -endif.

-ifndef(daily_task_tgr). 
-define(daily_task_tgr, daily_task_tgr).
 -endif.

-ifndef(task_tgr_builder). 
-define(task_tgr_builder, task_tgr_builder).
 -endif.

-ifndef(lvl_tgr). 
-define(lvl_tgr, lvl_tgr).
 -endif.

-ifndef(sundry_tgr). 
-define(sundry_tgr, sundry_tgr).
 -endif.

-ifndef(single_dig_tgr). 
-define(single_dig_tgr, single_dig_tgr).
 -endif.

-ifndef(accept_newbie_guide_task_tgr). 
-define(accept_newbie_guide_task_tgr, accept_newbie_guide_task_tgr).
 -endif.

-ifndef(submit_task_trigger_tgr). 
-define(submit_task_trigger_tgr, submit_task_trigger_tgr).
 -endif.

-ifndef(talk_npc_tgr). 
-define(talk_npc_tgr, talk_npc_tgr).
 -endif.

-ifndef(task_mng_new). 
-define(task_mng_new, task_mng_new).
 -endif.

-ifndef(task_open_fun). 
-define(task_open_fun, task_open_fun).
 -endif.

-ifndef(task_handle_client). 
-define(task_handle_client, task_handle_client).
 -endif.

-ifndef(payment_system). 
-define(payment_system, payment_system).
 -endif.

-ifndef(player_payment). 
-define(player_payment, player_payment).
 -endif.

-ifndef(equipment_mng). 
-define(equipment_mng, equipment_mng).
 -endif.

-ifndef(equip_system). 
-define(equip_system, equip_system).
 -endif.

-ifndef(nine_lottery_mng). 
-define(nine_lottery_mng, nine_lottery_mng).
 -endif.

-ifndef(chat_mng). 
-define(chat_mng, chat_mng).
 -endif.

-ifndef(notice_system). 
-define(notice_system, notice_system).
 -endif.

-ifndef(seller_mng). 
-define(seller_mng, seller_mng).
 -endif.

-ifndef(rank_mng). 
-define(rank_mng, rank_mng).
 -endif.

-ifndef(server_login_prize_mng). 
-define(server_login_prize_mng, server_login_prize_mng).
 -endif.

-ifndef(main_instance_rank_server). 
-define(main_instance_rank_server, main_instance_rank_server).
 -endif.

-ifndef(main_ins_util). 
-define(main_ins_util, main_ins_util).
 -endif.

-ifndef(main_ins_team_mod). 
-define(main_ins_team_mod, main_ins_team_mod).
 -endif.

-ifndef(main_instance_mng). 
-define(main_instance_mng, main_instance_mng).
 -endif.

-ifndef(main_ins_mod). 
-define(main_ins_mod, main_ins_mod).
 -endif.

-ifndef(main_ins). 
-define(main_ins, main_ins).
 -endif.

-ifndef(shop_mng). 
-define(shop_mng, shop_mng).
 -endif.

-ifndef(shop_system). 
-define(shop_system, shop_system).
 -endif.

-ifndef(gem_mng). 
-define(gem_mng, gem_mng).
 -endif.

-ifndef(gem). 
-define(gem, gem).
 -endif.

-ifndef(crown_mng). 
-define(crown_mng, crown_mng).
 -endif.

-ifndef(crown_new_mng). 
-define(crown_new_mng, crown_new_mng).
 -endif.

-ifndef(mall_server). 
-define(mall_server, mall_server).
 -endif.

-ifndef(mall_mng). 
-define(mall_mng, mall_mng).
 -endif.

-ifndef(skill_mng). 
-define(skill_mng, skill_mng).
 -endif.

-ifndef(abyss_server). 
-define(abyss_server, abyss_server).
 -endif.

-ifndef(abyss_mng). 
-define(abyss_mng, abyss_mng).
 -endif.

-ifndef(mail_mng). 
-define(mail_mng, mail_mng).
 -endif.

-ifndef(pet_new_mng). 
-define(pet_new_mng, pet_new_mng).
 -endif.

-ifndef(pet_global_server). 
-define(pet_global_server, pet_global_server).
 -endif.

-ifndef(pet_mng). 
-define(pet_mng, pet_mng).
 -endif.

-ifndef(goods_system). 
-define(goods_system, goods_system).
 -endif.

-ifndef(goods_mng). 
-define(goods_mng, goods_mng).
 -endif.

-ifndef(use_goods). 
-define(use_goods, use_goods).
 -endif.

-ifndef(alchemy_mng). 
-define(alchemy_mng, alchemy_mng).
 -endif.

-ifndef(scene_client_mng). 
-define(scene_client_mng, scene_client_mng).
 -endif.

-ifndef(scene_mng). 
-define(scene_mng, scene_mng).
 -endif.

-ifndef(gongcheng_mng). 
-define(gongcheng_mng, gongcheng_mng).
 -endif.

-ifndef(api). 
-define(api, api).
 -endif.

-ifndef(system_gm). 
-define(system_gm, system_gm).
 -endif.

-ifndef(gm_mng). 
-define(gm_mng, gm_mng).
 -endif.

-ifndef(ride_mng). 
-define(ride_mng, ride_mng).
 -endif.

-ifndef(arena_mng). 
-define(arena_mng, arena_mng).
 -endif.

-ifndef(arena_p2e). 
-define(arena_p2e, arena_p2e).
 -endif.

-ifndef(arena_p2p). 
-define(arena_p2p, arena_p2p).
 -endif.

-ifndef(arena_m_p2p). 
-define(arena_m_p2p, arena_m_p2p).
 -endif.

-ifndef(arena_util). 
-define(arena_util, arena_util).
 -endif.

-ifndef(arena_compete). 
-define(arena_compete, arena_compete).
 -endif.

-ifndef(arena_handle_client). 
-define(arena_handle_client, arena_handle_client).
 -endif.

-ifndef(bounty_mng). 
-define(bounty_mng, bounty_mng).
 -endif.

-ifndef(bounty_server). 
-define(bounty_server, bounty_server).
 -endif.

-ifndef(recharge_reward_mng). 
-define(recharge_reward_mng, recharge_reward_mng).
 -endif.

-ifndef(single_dig). 
-define(single_dig, single_dig).
 -endif.

-ifndef(treasure_map). 
-define(treasure_map, treasure_map).
 -endif.

-ifndef(vip_new_mng). 
-define(vip_new_mng, vip_new_mng).
 -endif.

-ifndef(pay_goods_part). 
-define(pay_goods_part, pay_goods_part).
 -endif.

-ifndef(sinks_state_part). 
-define(sinks_state_part, sinks_state_part).
 -endif.

-ifndef(charge_reward_part). 
-define(charge_reward_part, charge_reward_part).
 -endif.

-ifndef(login_prize_part). 
-define(login_prize_part, login_prize_part).
 -endif.

-ifndef(achievement_mng). 
-define(achievement_mng, achievement_mng).
 -endif.

-ifndef(title_mng). 
-define(title_mng, title_mng).
 -endif.

-ifndef(accomplishments_mng). 
-define(accomplishments_mng, accomplishments_mng).
 -endif.

-ifndef(accomplishments_plugin). 
-define(accomplishments_plugin, accomplishments_plugin).
 -endif.

-ifndef(game_res). 
-define(game_res, game_res).
 -endif.

-ifndef(entity_factory). 
-define(entity_factory, entity_factory).
 -endif.

-ifndef(main_star_shop_mng). 
-define(main_star_shop_mng, main_star_shop_mng).
 -endif.

-ifndef(auth_qq). 
-define(auth_qq, auth_qq).
 -endif.

-ifndef(attenuation). 
-define(attenuation, attenuation).
 -endif.

-ifndef(player). 
-define(player, player).
 -endif.

-ifndef(attr). 
-define(attr, attr).
 -endif.

-ifndef(player_prop_zip_key). 
-define(player_prop_zip_key, player_prop_zip_key).
 -endif.

-ifndef(attr_algorithm). 
-define(attr_algorithm, attr_algorithm).
 -endif.

-ifndef(attr_new). 
-define(attr_new, attr_new).
 -endif.

-ifndef(player_base_data). 
-define(player_base_data, player_base_data).
 -endif.

-ifndef(player_mng). 
-define(player_mng, player_mng).
 -endif.

-ifndef(player_mods_manager). 
-define(player_mods_manager, player_mods_manager).
 -endif.

-ifndef(account). 
-define(account, account).
 -endif.

-ifndef(player_handle_client). 
-define(player_handle_client, player_handle_client).
 -endif.

-ifndef(time_bucket). 
-define(time_bucket, time_bucket).
 -endif.

-ifndef(client_need_global_remove). 
-define(client_need_global_remove, client_need_global_remove).
 -endif.

-ifndef(goods_bucket). 
-define(goods_bucket, goods_bucket).
 -endif.

-ifndef(bucket_sync). 
-define(bucket_sync, bucket_sync).
 -endif.

-ifndef(goods_iterator). 
-define(goods_iterator, goods_iterator).
 -endif.

-ifndef(goods_pos_iterator). 
-define(goods_pos_iterator, goods_pos_iterator).
 -endif.

-ifndef(goods_id_iterator). 
-define(goods_id_iterator, goods_id_iterator).
 -endif.

-ifndef(goods_bid_iterator). 
-define(goods_bid_iterator, goods_bid_iterator).
 -endif.

-ifndef(team_server). 
-define(team_server, team_server).
 -endif.

-ifndef(card). 
-define(card, card).
 -endif.

-ifndef(card_mng). 
-define(card_mng, card_mng).
 -endif.

-ifndef(card_svr). 
-define(card_svr, card_svr).
 -endif.

-ifndef(server_res_eng). 
-define(server_res_eng, server_res_eng).
 -endif.

-ifndef(impact_ranking_list_service). 
-define(impact_ranking_list_service, impact_ranking_list_service).
 -endif.

-ifndef(impact_ranking_list_handle_client). 
-define(impact_ranking_list_handle_client, impact_ranking_list_handle_client).
 -endif.

-ifndef(freely_fall_speed). 
-define(freely_fall_speed, freely_fall_speed).
 -endif.

-ifndef(scene_agent). 
-define(scene_agent, scene_agent).
 -endif.

-ifndef(scene_eng). 
-define(scene_eng, scene_eng).
 -endif.

-ifndef(scene_player_plugin). 
-define(scene_player_plugin, scene_player_plugin).
 -endif.

-ifndef(scene_mod). 
-define(scene_mod, scene_mod).
 -endif.

-ifndef(scene_type_mod). 
-define(scene_type_mod, scene_type_mod).
 -endif.

-ifndef(xml_parse). 
-define(xml_parse, xml_parse).
 -endif.

-ifndef(buff_util). 
-define(buff_util, buff_util).
 -endif.

-ifndef(buff_plug_erudition). 
-define(buff_plug_erudition, buff_plug_erudition).
 -endif.

-ifndef(buff_plug_attrs). 
-define(buff_plug_attrs, buff_plug_attrs).
 -endif.

-ifndef(buff_plug_hurt). 
-define(buff_plug_hurt, buff_plug_hurt).
 -endif.

-ifndef(buff_plug_tag). 
-define(buff_plug_tag, buff_plug_tag).
 -endif.

-ifndef(buff_plug_move). 
-define(buff_plug_move, buff_plug_move).
 -endif.

-ifndef(buff_plug_group). 
-define(buff_plug_group, buff_plug_group).
 -endif.

-ifndef(buff_plug_hudun). 
-define(buff_plug_hudun, buff_plug_hudun).
 -endif.

-ifndef(buff_plug_reset). 
-define(buff_plug_reset, buff_plug_reset).
 -endif.

-ifndef(buff_plug_passive). 
-define(buff_plug_passive, buff_plug_passive).
 -endif.

-ifndef(buff_plug_release). 
-define(buff_plug_release, buff_plug_release).
 -endif.

-ifndef(buff_plug_halo). 
-define(buff_plug_halo, buff_plug_halo).
 -endif.

-ifndef(buff_plug_addattr). 
-define(buff_plug_addattr, buff_plug_addattr).
 -endif.

-ifndef(buff_system). 
-define(buff_system, buff_system).
 -endif.

-ifndef(skill_modify_util). 
-define(skill_modify_util, skill_modify_util).
 -endif.

-ifndef(pl_util). 
-define(pl_util, pl_util).
 -endif.

-ifndef(pl_fsm). 
-define(pl_fsm, pl_fsm).
 -endif.

-ifndef(plug_stiff). 
-define(plug_stiff, plug_stiff).
 -endif.

-ifndef(plug_beat_horizontal). 
-define(plug_beat_horizontal, plug_beat_horizontal).
 -endif.

-ifndef(plug_path_teleport). 
-define(plug_path_teleport, plug_path_teleport).
 -endif.

-ifndef(plug_moving). 
-define(plug_moving, plug_moving).
 -endif.

-ifndef(bullet_attack_area). 
-define(bullet_attack_area, bullet_attack_area).
 -endif.

-ifndef(plug_ba_ti). 
-define(plug_ba_ti, plug_ba_ti).
 -endif.

-ifndef(plug_buff). 
-define(plug_buff, plug_buff).
 -endif.

-ifndef(plug_jumping). 
-define(plug_jumping, plug_jumping).
 -endif.

-ifndef(plug_wait_for_moving). 
-define(plug_wait_for_moving, plug_wait_for_moving).
 -endif.

-ifndef(plug_dizzy). 
-define(plug_dizzy, plug_dizzy).
 -endif.

-ifndef(plug_attack). 
-define(plug_attack, plug_attack).
 -endif.

-ifndef(plug_beat_vertical). 
-define(plug_beat_vertical, plug_beat_vertical).
 -endif.

-ifndef(scene_monster). 
-define(scene_monster, scene_monster).
 -endif.

-ifndef(monster_group). 
-define(monster_group, monster_group).
 -endif.

-ifndef(buff_plugin_monster_hp). 
-define(buff_plugin_monster_hp, buff_plugin_monster_hp).
 -endif.

-ifndef(monster_ai_default). 
-define(monster_ai_default, monster_ai_default).
 -endif.

-ifndef(pet_ai). 
-define(pet_ai, pet_ai).
 -endif.

-ifndef(monster_script). 
-define(monster_script, monster_script).
 -endif.

-ifndef(monster_show). 
-define(monster_show, monster_show).
 -endif.

-ifndef(buff_plugin_monster_spirit_stat). 
-define(buff_plugin_monster_spirit_stat, buff_plugin_monster_spirit_stat).
 -endif.

-ifndef(monster_skill_mng). 
-define(monster_skill_mng, monster_skill_mng).
 -endif.

-ifndef(scene_monster_buff). 
-define(scene_monster_buff, scene_monster_buff).
 -endif.

-ifndef(map_agent). 
-define(map_agent, map_agent).
 -endif.

-ifndef(agent_util). 
-define(agent_util, agent_util).
 -endif.

-ifndef(bullet_agent). 
-define(bullet_agent, bullet_agent).
 -endif.

-ifndef(fight_interface). 
-define(fight_interface, fight_interface).
 -endif.

-ifndef(damage). 
-define(damage, damage).
 -endif.

-ifndef(scene_fight). 
-define(scene_fight, scene_fight).
 -endif.

-ifndef(scene_agent_factory). 
-define(scene_agent_factory, scene_agent_factory).
 -endif.

-ifndef(move_debug_tgr). 
-define(move_debug_tgr, move_debug_tgr).
 -endif.

-ifndef(move_x_tgr). 
-define(move_x_tgr, move_x_tgr).
 -endif.

-ifndef(move_util). 
-define(move_util, move_util).
 -endif.

-ifndef(move_path_tgr). 
-define(move_path_tgr, move_path_tgr).
 -endif.

-ifndef(move_tgr_util). 
-define(move_tgr_util, move_tgr_util).
 -endif.

-ifndef(move_xy_tgr). 
-define(move_xy_tgr, move_xy_tgr).
 -endif.

-ifndef(move_y_tgr). 
-define(move_y_tgr, move_y_tgr).
 -endif.

-ifndef(move_h_tgr). 
-define(move_h_tgr, move_h_tgr).
 -endif.

-ifndef(bullet_attack_tgr). 
-define(bullet_attack_tgr, bullet_attack_tgr).
 -endif.

-ifndef(attack_area_tgr). 
-define(attack_area_tgr, attack_area_tgr).
 -endif.

-ifndef(dead_tgr). 
-define(dead_tgr, dead_tgr).
 -endif.

-ifndef(buf). 
-define(buf, buf).
 -endif.

-ifndef(effect). 
-define(effect, effect).
 -endif.

-ifndef(map_aoi). 
-define(map_aoi, map_aoi).
 -endif.

-ifndef(login_sort). 
-define(login_sort, login_sort).
 -endif.

-ifndef(map_observers). 
-define(map_observers, map_observers).
 -endif.

-ifndef(scene_map). 
-define(scene_map, scene_map).
 -endif.

-ifndef(map_block). 
-define(map_block, map_block).
 -endif.

-ifndef(map_hit). 
-define(map_hit, map_hit).
 -endif.

-ifndef(calculate_trajectory). 
-define(calculate_trajectory, calculate_trajectory).
 -endif.

-ifndef(map_point). 
-define(map_point, map_point).
 -endif.

-ifndef(scene_team). 
-define(scene_team, scene_team).
 -endif.

-ifndef(scene_normal). 
-define(scene_normal, scene_normal).
 -endif.

-ifndef(scene_arena). 
-define(scene_arena, scene_arena).
 -endif.

-ifndef(scene_main_ins). 
-define(scene_main_ins, scene_main_ins).
 -endif.

-ifndef(scene_pet). 
-define(scene_pet, scene_pet).
 -endif.

-ifndef(scene_drop). 
-define(scene_drop, scene_drop).
 -endif.

-ifndef(scene_device). 
-define(scene_device, scene_device).
 -endif.

-ifndef(scene_portal). 
-define(scene_portal, scene_portal).
 -endif.

-ifndef(scene_collect). 
-define(scene_collect, scene_collect).
 -endif.

-ifndef(scene_gm). 
-define(scene_gm, scene_gm).
 -endif.

-ifndef(scene). 
-define(scene, scene).
 -endif.

-ifndef(scene_player). 
-define(scene_player, scene_player).
 -endif.

-ifndef(agent_ai). 
-define(agent_ai, agent_ai).
 -endif.

-ifndef(arena_server). 
-define(arena_server, arena_server).
 -endif.

-ifndef(gwgc_server). 
-define(gwgc_server, gwgc_server).
 -endif.

-ifndef(team_svr). 
-define(team_svr, team_svr).
 -endif.

-ifndef(team_mod). 
-define(team_mod, team_mod).
 -endif.

-ifndef(team_mng). 
-define(team_mng, team_mng).
 -endif.

-ifndef(jpush_service). 
-define(jpush_service, jpush_service).
 -endif.

-ifndef(jpush_http). 
-define(jpush_http, jpush_http).
 -endif.

-ifndef(guild_boss). 
-define(guild_boss, guild_boss).
 -endif.

-ifndef(guild_mining_mng). 
-define(guild_mining_mng, guild_mining_mng).
 -endif.

-ifndef(guild_mining_server). 
-define(guild_mining_server, guild_mining_server).
 -endif.

-ifndef(guild_service). 
-define(guild_service, guild_service).
 -endif.

-ifndef(guild_handle_client). 
-define(guild_handle_client, guild_handle_client).
 -endif.

-ifndef(guild_mng). 
-define(guild_mng, guild_mng).
 -endif.

-ifndef(double_prize_server). 
-define(double_prize_server, double_prize_server).
 -endif.

-ifndef(payment_confirm). 
-define(payment_confirm, payment_confirm).
 -endif.

-ifndef(timer_trigger_server). 
-define(timer_trigger_server, timer_trigger_server).
 -endif.

-ifndef(open_server_happy_mng). 
-define(open_server_happy_mng, open_server_happy_mng).
 -endif.

-ifndef(sky_service). 
-define(sky_service, sky_service).
 -endif.

-ifndef(sky_handle_client). 
-define(sky_handle_client, sky_handle_client).
 -endif.

-ifndef(sky_mng). 
-define(sky_mng, sky_mng).
 -endif.

-ifndef(nine_lottery_server). 
-define(nine_lottery_server, nine_lottery_server).
 -endif.

-ifndef(honest_user_mng). 
-define(honest_user_mng, honest_user_mng).
 -endif.

-ifndef(phase_achievement_mng). 
-define(phase_achievement_mng, phase_achievement_mng).
 -endif.

-ifndef(camp_mng). 
-define(camp_mng, camp_mng).
 -endif.

-ifndef(camp_service). 
-define(camp_service, camp_service).
 -endif.

-ifndef(camp_handle_client). 
-define(camp_handle_client, camp_handle_client).
 -endif.

-ifndef(event_server). 
-define(event_server, event_server).
 -endif.

-ifndef(com_mod_sup). 
-define(com_mod_sup, com_mod_sup).
 -endif.

-ifndef(scene_sup). 
-define(scene_sup, scene_sup).
 -endif.

-ifndef(player_sup). 
-define(player_sup, player_sup).
 -endif.

-ifndef(listener_cmd). 
-define(listener_cmd, listener_cmd).
 -endif.

-ifndef(load_cfg_gwgc). 
-define(load_cfg_gwgc, load_cfg_gwgc).
 -endif.

-ifndef(load_cfg_ride). 
-define(load_cfg_ride, load_cfg_ride).
 -endif.

-ifndef(load_cfg_crown). 
-define(load_cfg_crown, load_cfg_crown).
 -endif.

-ifndef(load_cfg_mall). 
-define(load_cfg_mall, load_cfg_mall).
 -endif.

-ifndef(load_cfg_card). 
-define(load_cfg_card, load_cfg_card).
 -endif.

-ifndef(load_richang_task). 
-define(load_richang_task, load_richang_task).
 -endif.

-ifndef(load_spirit_attr). 
-define(load_spirit_attr, load_spirit_attr).
 -endif.

-ifndef(load_cfg_scene_collect). 
-define(load_cfg_scene_collect, load_cfg_scene_collect).
 -endif.

-ifndef(load_arena_cfg). 
-define(load_arena_cfg, load_arena_cfg).
 -endif.

-ifndef(load_task_open_fun). 
-define(load_task_open_fun, load_task_open_fun).
 -endif.

-ifndef(load_cfg_shop). 
-define(load_cfg_shop, load_cfg_shop).
 -endif.

-ifndef(misc_cfg). 
-define(misc_cfg, misc_cfg).
 -endif.

-ifndef(load_cfg_monster_group). 
-define(load_cfg_monster_group, load_cfg_monster_group).
 -endif.

-ifndef(load_cfg_mining_prize). 
-define(load_cfg_mining_prize, load_cfg_mining_prize).
 -endif.

-ifndef(load_cfg_guild). 
-define(load_cfg_guild, load_cfg_guild).
 -endif.

-ifndef(load_battle_coef). 
-define(load_battle_coef, load_battle_coef).
 -endif.

-ifndef(load_cfg_buff). 
-define(load_cfg_buff, load_cfg_buff).
 -endif.

-ifndef(load_cfg_scene). 
-define(load_cfg_scene, load_cfg_scene).
 -endif.

-ifndef(load_cfg_daily_activity). 
-define(load_cfg_daily_activity, load_cfg_daily_activity).
 -endif.

-ifndef(load_vip_right). 
-define(load_vip_right, load_vip_right).
 -endif.

-ifndef(load_cfg_achievement). 
-define(load_cfg_achievement, load_cfg_achievement).
 -endif.

-ifndef(load_cfg_server_login_prize). 
-define(load_cfg_server_login_prize, load_cfg_server_login_prize).
 -endif.

-ifndef(load_dig_goods). 
-define(load_dig_goods, load_dig_goods).
 -endif.

-ifndef(load_offset_goods). 
-define(load_offset_goods, load_offset_goods).
 -endif.

-ifndef(load_cfg_city). 
-define(load_cfg_city, load_cfg_city).
 -endif.

-ifndef(load_cfg_cd_key_rule). 
-define(load_cfg_cd_key_rule, load_cfg_cd_key_rule).
 -endif.

-ifndef(load_cfg_fishing_prize). 
-define(load_cfg_fishing_prize, load_cfg_fishing_prize).
 -endif.

-ifndef(load_cfg_main_star_shop). 
-define(load_cfg_main_star_shop, load_cfg_main_star_shop).
 -endif.

-ifndef(prize). 
-define(prize, prize).
 -endif.

-ifndef(load_cfg_sky). 
-define(load_cfg_sky, load_cfg_sky).
 -endif.

-ifndef(load_cfg_monster_show). 
-define(load_cfg_monster_show, load_cfg_monster_show).
 -endif.

-ifndef(load_cfg_mail). 
-define(load_cfg_mail, load_cfg_mail).
 -endif.

-ifndef(load_segments). 
-define(load_segments, load_segments).
 -endif.

-ifndef(load_cfg_scene_map). 
-define(load_cfg_scene_map, load_cfg_scene_map).
 -endif.

-ifndef(load_cfg_seller). 
-define(load_cfg_seller, load_cfg_seller).
 -endif.

-ifndef(load_cfg_recharge_prize). 
-define(load_cfg_recharge_prize, load_cfg_recharge_prize).
 -endif.

-ifndef(load_cfg_pet). 
-define(load_cfg_pet, load_cfg_pet).
 -endif.

-ifndef(load_item). 
-define(load_item, load_item).
 -endif.

-ifndef(load_nine_lottery_cfg). 
-define(load_nine_lottery_cfg, load_nine_lottery_cfg).
 -endif.

-ifndef(load_black_shop_cfg). 
-define(load_black_shop_cfg, load_black_shop_cfg).
 -endif.

-ifndef(load_scene_monster). 
-define(load_scene_monster, load_scene_monster).
 -endif.

-ifndef(load_phase_ac). 
-define(load_phase_ac, load_phase_ac).
 -endif.

-ifndef(load_cfg_emits). 
-define(load_cfg_emits, load_cfg_emits).
 -endif.

-ifndef(load_cfg_guild_saint). 
-define(load_cfg_guild_saint, load_cfg_guild_saint).
 -endif.

-ifndef(load_equip_expand). 
-define(load_equip_expand, load_equip_expand).
 -endif.

-ifndef(load_cfg_compound). 
-define(load_cfg_compound, load_cfg_compound).
 -endif.

-ifndef(load_auction_ai). 
-define(load_auction_ai, load_auction_ai).
 -endif.

-ifndef(load_robot_cfg). 
-define(load_robot_cfg, load_robot_cfg).
 -endif.

-ifndef(load_cfg_skill). 
-define(load_cfg_skill, load_cfg_skill).
 -endif.

-ifndef(load_cfg_shangjin_task). 
-define(load_cfg_shangjin_task, load_cfg_shangjin_task).
 -endif.

-ifndef(load_cfg_scene_drop). 
-define(load_cfg_scene_drop, load_cfg_scene_drop).
 -endif.

-ifndef(npc). 
-define(npc, npc).
 -endif.

-ifndef(load_abyss_integral_reward). 
-define(load_abyss_integral_reward, load_abyss_integral_reward).
 -endif.

-ifndef(load_equip_change). 
-define(load_equip_change, load_equip_change).
 -endif.

-ifndef(load_lvl_prize). 
-define(load_lvl_prize, load_lvl_prize).
 -endif.

-ifndef(load_vip_new). 
-define(load_vip_new, load_vip_new).
 -endif.

-ifndef(load_dial_prize). 
-define(load_dial_prize, load_dial_prize).
 -endif.

-ifndef(load_cfg_main_ins). 
-define(load_cfg_main_ins, load_cfg_main_ins).
 -endif.

-ifndef(load_unlock). 
-define(load_unlock, load_unlock).
 -endif.

-ifndef(load_cfg_punchs). 
-define(load_cfg_punchs, load_cfg_punchs).
 -endif.

-ifndef(load_cfg_open_server_happy). 
-define(load_cfg_open_server_happy, load_cfg_open_server_happy).
 -endif.

-ifndef(load_cfg_arena_robot). 
-define(load_cfg_arena_robot, load_cfg_arena_robot).
 -endif.

-ifndef(load_simple_task). 
-define(load_simple_task, load_simple_task).
 -endif.

-ifndef(load_cfg_broadcast). 
-define(load_cfg_broadcast, load_cfg_broadcast).
 -endif.

-ifndef(load_cfg_scene_device). 
-define(load_cfg_scene_device, load_cfg_scene_device).
 -endif.

-ifndef(load_cfg_impact_ranking_list). 
-define(load_cfg_impact_ranking_list, load_cfg_impact_ranking_list).
 -endif.

-ifndef(load_cfg_suit_fenjie). 
-define(load_cfg_suit_fenjie, load_cfg_suit_fenjie).
 -endif.

-ifndef(load_cfg_gem). 
-define(load_cfg_gem, load_cfg_gem).
 -endif.

-ifndef(load_cfg_monster_script). 
-define(load_cfg_monster_script, load_cfg_monster_script).
 -endif.

-ifndef(load_cfg_title). 
-define(load_cfg_title, load_cfg_title).
 -endif.

-ifndef(load_course). 
-define(load_course, load_course).
 -endif.

-ifndef(load_cfg_new_pet). 
-define(load_cfg_new_pet, load_cfg_new_pet).
 -endif.

-ifndef(cost). 
-define(cost, cost).
 -endif.

-ifndef(load_cfg_halo). 
-define(load_cfg_halo, load_cfg_halo).
 -endif.

-ifndef(load_pay_prize). 
-define(load_pay_prize, load_pay_prize).
 -endif.

-ifndef(load_double_prize). 
-define(load_double_prize, load_double_prize).
 -endif.

-ifndef(load_cfg_daily_task). 
-define(load_cfg_daily_task, load_cfg_daily_task).
 -endif.

-ifndef(load_cfg_task_buff_pool). 
-define(load_cfg_task_buff_pool, load_cfg_task_buff_pool).
 -endif.

-ifndef(load_cfg_camp). 
-define(load_cfg_camp, load_cfg_camp).
 -endif.

-ifndef(load_honest_user_cfg). 
-define(load_honest_user_cfg, load_honest_user_cfg).
 -endif.

-ifndef(load_cfg_time_trigger). 
-define(load_cfg_time_trigger, load_cfg_time_trigger).
 -endif.

-ifndef(load_cost). 
-define(load_cost, load_cost).
 -endif.

-ifndef(load_day_login_prize). 
-define(load_day_login_prize, load_day_login_prize).
 -endif.

-ifndef(load_pay_order). 
-define(load_pay_order, load_pay_order).
 -endif.

-ifndef(load_cfg_scene_portal). 
-define(load_cfg_scene_portal, load_cfg_scene_portal).
 -endif.

-ifndef(load_cfg_gates). 
-define(load_cfg_gates, load_cfg_gates).
 -endif.

-ifndef(load_cfg_guild_boss). 
-define(load_cfg_guild_boss, load_cfg_guild_boss).
 -endif.

-ifndef(load_task_progress). 
-define(load_task_progress, load_task_progress).
 -endif.

-ifndef(load_server_info). 
-define(load_server_info, load_server_info).
 -endif.

-ifndef(load_career_attr). 
-define(load_career_attr, load_career_attr).
 -endif.

-ifndef(load_alchemy). 
-define(load_alchemy, load_alchemy).
 -endif.

-ifndef(mobile_game_sup). 
-define(mobile_game_sup, mobile_game_sup).
 -endif.

-ifndef(mysql_db). 
-define(mysql_db, mysql_db).
 -endif.

-ifndef(my_ets). 
-define(my_ets, my_ets).
 -endif.

-ifndef(virtual_db). 
-define(virtual_db, virtual_db).
 -endif.

-ifndef(erlang_db). 
-define(erlang_db, erlang_db).
 -endif.

-ifndef(mobile_game_app). 
-define(mobile_game_app, mobile_game_app).
 -endif.

