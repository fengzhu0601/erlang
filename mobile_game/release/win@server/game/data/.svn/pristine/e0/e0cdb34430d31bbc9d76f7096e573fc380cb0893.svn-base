SetAIType(AITypes.normal)

Radiuses =
{
    strollRadius = 50,
    chaseRadius = 50,
    chaseSpeed = {1.1, 1.4}
}

SetRadiuses(Radiuses)

AIDecisions =
{
    { denefit = 10, mutex = 5, events = {}, conditions = {CheckLifeTime(1)}, actions = {{ action = SendEventToMonster({{4002001,1}}, 1, 1, {41}), number = 1}}}
}

LoadConfig(AIDecisions)